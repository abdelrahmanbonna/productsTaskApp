import 'package:get/get.dart';

class SplashBinding extends Bindings {
  @override
  void dependencies() {
    // Register splash screen dependencies
    // Example: Get.lazyPut(() => SplashController());
  }
}
